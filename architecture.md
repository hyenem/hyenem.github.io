# BudsAI — 전체 시스템 아키텍처 장표 (초안)

> **용도**: 이 문서를 바탕으로 이후 HTML/SVG 기술설명 장표를 제작한다.
> **상태**: 초안 — 슬라이드 구성 + 레이어별 스펙. 빨강 ⚠️ 표시는 기존 자료 간 수치 불일치라 **같이 확정 필요**.
> **출처**: proposal / packet / firmware / host-bridge / server-api / bnep-slide / opus-slide / vad-slide / concept / scenarios (repo 내 기존 슬라이드)
> **기준일**: 2026-05-30

---

## 0. 한 줄 정의 (모든 장표의 출발점)

> **BudsAI** = 버즈가 **방금까지의 소리 환경(이어폰 출력=내부 + 주변음=외부) 30초**를 항상 링버퍼에 보관하다가,
> 사용자가 질문(발화)하는 순간 **그 컨텍스트 사운드 + 발화**를 함께 AI에 보내는 이어버드.
>
> 시장의 다른 모든 제품은 "질문 시점부터의 마이크 입력"만 보낸다. **"방금 그거"를 아는 건 BudsAI뿐.**

핵심 데이터 흐름은 **호스트가 Galaxy 폰이냐 아니냐에 따라 두 갈래**다 (→ 자세히는 §0.5). 표지 다음 장 메인 다이어그램은 이 이중 경로를 보여줘야 한다.

```
🔵 Galaxy 폰 경로 (폰 = 스마트 프록시, 폰이 페이로드 봄)
[버즈] ─BT audio─▶ [Galaxy 폰] ─HTTPS·JWT─▶ [서버] ──(text 답변 + action item)──┐
  ▲ 30초 링버퍼      ▲ DB저장·session_id매핑·TTS(VITS)·액션라우팅              │
  └────────────────── 폰이 TTS 생성해 BT로 버즈에 재생 ◀───────────────────────┘

🔴 비-Galaxy 호스트 경로 (호스트 = dumb pipe, 아무도 페이로드 못 봄)
[버즈] ─BT PAN─▶ [호스트 BNEP/IP 라우팅] ─mTLS E2E 통과─▶ [서버] ──(TTS까지 생성)──┐
  ▲ 기기 인증서·mTLS 종단  ▲ passthrough only, 페이로드 비공개                    │
  └────────────────── 서버 TTS가 호스트 통과해 버즈 재생 ◀────────────────────────┘
```

---

## 0.5 데이터 흐름의 두 갈래 — Galaxy vs 비-Galaxy `diagram-combined.svg 기준`

**가장 중요한 구조.** 호스트가 자사(Galaxy) 폰이냐 아니냐에 따라 **신뢰 경계 · 인증 · TTS 생성 위치 · 호스트 역할**이 전부 달라진다. 같은 버즈 한 쌍이지만 동작 모델이 둘이다.

| 항목 | 🔵 Galaxy 폰 경로 | 🔴 비-Galaxy 호스트 경로 |
|------|------------------|------------------------|
| **호스트 역할** | **스마트 프록시** (능동 참여) | **Dumb Pipe** (BNEP IP 라우팅만) |
| **버즈↔호스트 링크** | BT (audio) — 폰이 오디오 받음 | BT PAN — IP 패킷만 전달 |
| **인증** | **HTTPS · JWT** (폰의 OAuth 토큰) | **HTTPS · mTLS E2E** (버즈 기기 인증서) |
| **세션 endpoint** | Galaxy 폰 (앱) | 버즈 자체 |
| **호스트가 페이로드를** | **본다** (자사 기기라 신뢰) | **못 본다** (E2E 암호, zero-knowledge) |
| **요청 시 호스트 처리** | local DB 저장(SQLite, status=wait), 패킷 생성 | passthrough only |
| **서버 응답 내용** | **text 답변 + action item** (JSON) | **TTS 오디오까지 완성** |
| **TTS 생성 위치** | **폰에서 직접** (VITS · 24kbps LC3) | **서버에서** 생성 (LC3) |
| **응답 시 호스트 처리** | session_id로 DB 매핑, action item 후속처리(Calendar·SmartThings…), TTS 생성 | 호스트 통과 (페이로드 비공개) |
| **버즈 출력** | 폰 TTS를 BT로 받아 재생 | 서버 TTS를 호스트 통과로 받아 재생 |

**왜 이렇게 나누나 (장표용 논리)**
- 🔵 **Galaxy**: 자사 폰은 신뢰할 수 있으므로 폰을 능동 활용 → ① 서버는 가벼운 text+action만, **TTS는 on-device(VITS)로 서버 비용·지연↓** ② 폰이 Calendar·SmartThings·Notes로 **생태계 액션 라우팅** ③ session_id 매핑으로 히스토리·연속성 관리.
- 🔴 **비-Galaxy**: 남의 기기(노트북·렌터카·공유PC)는 신뢰 불가 → 버즈 기기 인증서(SoC 키스토어)로 **mTLS 종단을 버즈에 두고** 호스트는 평문을 못 보는 **dumb pipe**. 대신 폰의 도움이 없으니 **서버가 TTS까지 전부 생성**.

> 한 줄 요약: **"신뢰 가능한 자사 폰이 있으면 폰이 일을 나눠 갖고(JWT·on-device TTS·액션라우팅), 없으면 버즈가 직접 서버와 E2E로 끝낸다(mTLS·서버 TTS·dumb pipe)."**

> ⚠️ **불일치 3 (확인 필요)**: 이전 다이어그램은 TTS를 Galaxy 경로에선 **폰(VITS 24kbps Opus)**, 비-Galaxy 경로에선 **서버**가 만든다고 그렸음. 그런데 server-api.html 원문은 TTS를 **서버가 Opus 16kHz ~24kbps로 생성·스트리밍**한다고 기술. → Galaxy 경로 TTS를 진짜 폰에서 만들지(VITS on-device) vs 서버가 만들고 폰은 라우팅만 할지 **확정 필요**. 둘 다 말이 되지만 장표 메시지가 갈림.

---

## 1. 슬라이드 구성안 (제안 — 10장)

| # | 제목 | 태그 | 한 줄 메시지 |
|---|------|------|-------------|
| 01 | 한 줄 컨셉 + 왜 | Concept | "이미 들은 사운드가 컨텍스트" — 유일한 본질 차별점 |
| 02 | 전체 데이터 흐름 — **두 갈래** | Architecture | **Galaxy 폰 경로 vs 비-Galaxy 호스트 경로** 나란히 (§0.5) |
| 03 | Layer 1 — 버즈 펌웨어 | Firmware | 2트랙 캡처 + 30초 링버퍼 + VAD + SoC 보안 키스토어 |
| 04 | Layer 2 — 전송 (BT PAN/BNEP) | Transport | 표준 PAN만 사용 → 앱 설치 0, 호스트 무관 |
| 05 | Layer 2b — 호스트의 두 얼굴 | Host | **Galaxy=스마트 프록시 / 비-Galaxy=Dumb Pipe** 대비 |
| 06 | Layer 3 — 패킷 포맷 | Packet | 세션 메시지 / 2트랙 / 사이즈 / 연속성 |
| 07 | Layer 4 — 서버 AI 파이프라인 | Server | STT → 의도분류 8개 → 핸들러 → TTS |
| 08 | 보안 — mTLS 종단을 버즈에 (1~2장) | Security | TLS/mTLS란→핸드셰이크→E2E 원리→**사용자 체감 3개 vs 운영자 이득(더 큼)** 정직 분리 |
| 09 | 지연 예산 — ~3초 분해 | Latency | 버즈→호스트→서버→AI→재생 단계별 |
| 10 | Galaxy 생태계 (옵션 레이어) | Galaxy | 자동 라우팅 · Bixby Routines · 비-Galaxy도 풀기능 |

> 분량 조절: 발표가 짧으면 04+05 합치고 09를 02 안에 흡수해 7장으로 압축 가능.
> 보조 슬라이드(필요 시 삽입): **03 뒤 'VAD 자세히'**(§VAD), **04 앞 'Bluetooth 기초'**(§블루투스 기초) — 기술 청중이면 추가, 경영 발표면 생략.

---

## 2. 레이어별 정확한 스펙 (장표 본문 재료)

### Layer 1 — 버즈 펌웨어 `firmware.html / packet.html / vad-slide / opus-slide`

**2트랙 캡처 모델**
- **Track A (내부/외부 컨텍스트 사운드)**: "내가 처해 있던 소리 환경"을 상시 캡처 → **2채널 분리 LC3 인코딩**
  - **A-int (내부)** = 이어폰 출력(I2S DAC / LE Audio DL decomp) · **LC3 24 kbps mono**
  - **A-ext (외부)** = ANC 외부 마이크 주변음(ADC) · **LC3 24 kbps mono**
  - 보관: **각 채널 30초 → 컨텍스트 합계 ~180 KB** · 프레임 10ms (LC3FrameIndex, 30s/채널)
  - ⭐ **A-int 소스가 이미 LE Audio(LC3) DL이면 재인코딩 없이 네이티브 LC3 프레임 그대로 저장 → 트랜스코딩 0** (A-ext 외부 마이크·A2DP·아날로그 소스는 ADC→인코딩 필요)
- **Track B (사용자 발화)**: 음성 마이크 업링크(UL)
  - **LC3 24 kbps mono (16kHz wideband)** — LE Audio 통화 업링크가 쓰는 그 방식
  - 최소 500ms / 최대 8초 녹음

**VAD (발화 끝 감지 = 엔드포인팅)** — 용도·방식 상세는 아래 **§VAD 자세히**
- 용도: **트리거 이후** Track B를 녹음하다가 **발화가 끝나는 시점**을 잡아 자동 종료 (시작은 트리거가 담당)
- 방식: 가속도계/골전도 VAD 단일 — 재생음·주변소음 면역
- 침묵 타임아웃: **800 ms** → 녹음 종료 · 최소 500ms / 최대 8초

**트리거**
- 롱프레스 **2000 ms** → ARMED 상태, 150ms "띵" 효과음
- 트리거 감지 지연 **<50 ms**

**보안 종단**
- 디바이스 **X.509 인증서·개인키를 SoC 하드웨어 키스토어(TrustZone-M·KMU)**에 보관 (별도 SE 칩 아님 — 보안 섹션 §2.5~2.7)
- 버즈가 직접 서버와 **mTLS (TLS 1.3)** 핸드셰이크

**상태별 전류**: IDLE ~2mA / BUFFERING ~15mA / RECORDING ~25mA / TRANSMITTING ~30mA / PLAY ~15mA

> 비고: 기존 슬라이드(compare/packet/firmware)는 전부 **Opus** 기준으로 작성됨 → **전 계층 코덱을 LC3로 교체** 결정에 따라 슬라이드 카피 일괄 수정 필요.

---

### 코덱 — 왜 LC3인가 (신규 결정) `웹 검증 2026-05-30`

**결정**: Track A·B(및 가능하면 TTS)를 **LC3 (Low Complexity Communication Codec) · 전 스트림 24 kbps mono** 로 통일. Opus에서 변경.

#### 빌드업 — 오디오는 어디서 와서 왜 압축이 필수인가

**① 소리는 세 입력으로 들어와 두 트랙으로 묶인다 (캡처 흐름)**

버즈가 AI로 보낼 소리는 입력이 세 갈래지만, 경로 끝에선 **전부 PCM**이 되어 두 트랙으로 모인다:
- **Track A · 컨텍스트 사운드 — 내부/외부 2채널 분리 전송**
  - **A-int (내부)**: 이어폰으로 나가던 출력(음악·통화·영상)은 재생 위해 이미 압축이 풀린(**decomp**) 신호 → PCM
  - **A-ext (외부)**: ANC용 **외부 마이크**가 받은 주변 소리(카페 음악·대사 등) → **ADC(아날로그→디지털 변환)** → PCM
- **Track B · 사용자 발화** — "내가 한 질문"
  - **음성 마이크**가 받은 사용자 발화 → **ADC** → PCM

```
🔊 이어폰 출력(내부) ─decomp─▶ PCM ─LC3─▶ Track A-int (내부 채널)
🎙️ ANC 마이크(외부) ─ADC─────▶ PCM ─LC3─▶ Track A-ext (외부 채널)
🗣️ 음성 마이크(발화) ─ADC─────▶ PCM ─LC3─▶ Track B     (사용자 발화)
```

> **왜 믹스 안 하고 분리?** 분리하면 AI가 **"이어폰으로 듣던 콘텐츠(내부)"와 "주변 환경음(외부)"를 구분**해 처리할 수 있다 — 예: 내부=영상 대사 번역, 외부=카페에 흐르던 노래 식별. 믹스하면 둘이 섞여 분리가 불가능. **대가는 컨텍스트 용량이 약 2배** (아래 ② 표).

**PCM은 그냥 정수의 나열**이다. 48kHz라면 1초에 숫자 4만 8천 개, 각 숫자가 그 순간 진동판의 위치(예: 16비트면 −32768~32767)를 나타낼 뿐 — 구조도 압축도 없는 **생(raw) 데이터 호스**.

**② 비트레이트 결정 + 왜 압축이 필수인가**

**결정: 모든 오디오 스트림 = LC3 24 kbps (mono).** A-int·A-ext·B 전부 동일 — 복잡하게 나누지 않고 하나로.

**24kbps가 일상에서 어떤 소리냐 (근거):**
- 🎧 **Spotify '데이터 절약(저음질)' ≈ 24kbps** — 데이터 아낄 때 듣는 그 음질. 무슨 곡인지 바로 알아듣고 따라 부를 정도.
- 📻 **디지털 라디오(DAB)·인터넷 라디오 음악 채널**이 대략 이 대역(24~48kbps).
- 📞 **요즘 휴대폰 HD 음성통화 ≈ 13~24kbps** — 24kbps면 통화보다 또렷.

우리 목적은 **'감상'이 아니라 '식별·이해'**(무슨 노래·무슨 말인지 AI가 알면 됨)라서:
- 더 높이면(🎵 일반 MP3 128kbps) 음질만 좋아지고 **용량 낭비** — 식별엔 불필요.
- 더 낮추면(☎️ 옛 인터넷전화 8kbps) 음성은 되나 **음악 식별이 불안**.
- → **24kbps = 음악 식별 + 음성 인식 둘 다 안전한 sweet spot.** (음악 식별은 Shazam도 16kHz 저대역만 봐서 24kbps면 차고 넘침.)

**용량 — 압축이 왜 필수인지:**

| 스트림 | LC3 24kbps 용량 |
|---|---|
| A-int · 내부(이어폰 출력) 30초 | ~90 KB |
| A-ext · 외부(주변음) 30초 | ~90 KB |
| B · 사용자 발화 5초 | ~15 KB |
| **한 요청 합계** | **~200 KB** |

- 압축 안 하면? 생 PCM은 한 요청이 **~6 MB** → BT로 보내면 **40초 넘게** 걸려 제품 불가능.
- **LC3 24kbps면 ~200KB → ~2초** 전송. 압축은 옵션이 아니라 전제 조건이고, 그걸 **버즈 칩에 이미 있는 LC3로 공짜** 해결.

**버즈4 프로 코덱 지원 현황** (Bluetooth 6.1): **SBC · AAC · SSC UHQ(삼성 독자, 24bit/96kHz, 갤럭시 전용) · LC3(LE Audio)**. Opus는 미지원.

**LC3 핵심 스펙** (Bluetooth SIG, Fraunhofer IIS + Ericsson)
- Bluetooth **LE Audio의 필수(mandatory) 코덱** — 모든 LE Audio 칩에 이미 내장
- 샘플레이트 8/16/24/32/44.1/48 kHz · 프레임 7.5ms 또는 10ms · 비트뎁스 16/24/32
- 비트레이트 ~16 kbps ~ 345 kbps 가변
- 효율: **SBC 대비 절반 비트레이트로 동등 이상 품질** (LC3 160kbps > SBC 345kbps)

**비교 범위 = 갤럭시 버즈4 프로가 실제 지원하는 코덱 4종** (Bluetooth 6.1: SBC · AAC · SSC UHQ · LC3). ※ **Opus·aptX·LDAC은 버즈4 프로 미지원** → 비교 대상 아님.

| 버즈4프로 지원 코덱 | 종류 | 설계 지향 | 저비트레이트 효율 | UL(마이크 업링크) | 호스트 |
|---|---|---|---|---|---|
| **LC3** | **LE Audio 필수** | **압축효율** | **최고** (SBC 절반 비트레이트) | **◎ 양방향 네이티브** (CIS·HFP1.9 LC3-SWB) | 크로스벤더 |
| SSC UHQ | 삼성 독자(Seamless) | **고음질** (24bit/96kHz) = 고비트레이트 | 낮음 (압축 목적 아님) | ✗ DL 재생 전용 | **갤럭시 전용** |
| AAC | A2DP 옵션 | 범용 | 중간 | ✗ DL 위주 | 크로스 |
| SBC | A2DP 기본 | 호환성 | 낮음 (≥345kbps 필요) | △ 통화는 mSBC/CVSD 별도 | 크로스 |

**정직한 포인트 (이 범위에선 주장 성립)**
- 버즈4 프로 지원 코덱 **4종 중 저비트레이트 압축효율 1등은 LC3가 명확** → "**버즈4 프로가 쓰는 코덱 중 압축률이 가장 높다**"는 정확한 카피. (SSC UHQ는 압축이 아니라 24bit/96kHz **고음질용**, SBC/AAC는 효율 열위. Opus는 애초에 버즈4 프로 미지원이라 논외.)
- 우리가 필요한 건 **저비트레이트 컨텍스트 오디오 전송** → 지원 코덱 중 LC3가 분명한 최적. 과장 아님.
- 보강 근거: **4종 중 LC3만 UL/DL 양방향** → Track A(내부/외부 컨텍스트, 주로 DL)·Track B(사용자 발화, UL)를 한 코덱으로 통일. 나머지 3종은 DL 재생 전용(통화 UL은 mSBC/CVSD). + LE Audio 칩 **내장 필수 코덱**이라 추가 구현·라이선스 0, 내부 DL 소스가 LC3면 **재인코딩 없이 프레임 그대로 저장**.

**UL(업링크)에 원래 쓰이나? → YES (확인됨)**
- LE Audio의 **CIS(Connected Isochronous Stream)는 양방향 point-to-point** — 통화 시 이어버드 **마이크 업링크 음성을 LC3로 인코딩**해 폰으로 보냄.
- **HFP 1.9의 LC3-SWB(Super Wideband, ~14kHz)** 가 HD Voice+ 통화 업링크 표준. 게이밍 등 **마이크 입력 + 오디오 출력 동시(bidirectional)** 도 LC3.
- 즉 **Track B(사용자 발화 = 음성 마이크 UL)는 LC3의 원래 용도 그대로** → "버즈에서 UL에 원래 쓰는 코덱을 그대로 활용" 메시지 성립.

#### LC3는 어떻게 1/20로 줄이나 — 직관적 원리

생 PCM은 "1초에 숫자 4만 8천 개"지만, **사람 귀는 그 숫자를 다 듣지 않는다**. LC3의 핵심 직관은 **"들리는 것만, 들리는 만큼만 저장한다"**:

1. **시간 → 주파수로 바꿔 본다 (MDCT 변환)**: 파형 숫자 나열을 "어떤 음 높이가 얼마나 섞였나"로 변환. 소리는 결국 주파수의 합이라, 이 형태가 다루기 훨씬 쉽다.
2. **안 들리는 소리는 버린다 (심리음향 마스킹)**: 큰 소리 옆 작은 소리는 안 들리고(마스킹), 너무 높거나 낮은 대역엔 귀가 둔감하다 → **안 들리는 성분엔 비트를 아예 안 쓴다.**
3. **중요한 곳에 비트 몰아주기 (양자화·비트 할당)**: 잘 들리는 주파수는 정밀하게, 덜 중요한 곳은 거칠게 표현.
4. **남은 걸 한 번 더 짠다 (엔트로피 코딩)**: 자주 나오는 값에 짧은 코드를 부여해 추가 압축.

→ 결과: **귀엔 거의 똑같은데 데이터는 1/16~1/24.** LC3가 특히 잘하는 3가지:
- **(a) 저지연** — 7.5/10ms 프레임이라 통화·실시간 질의에 적합
- **(b) 저복잡도** — 작은 버즈 DSP에서 가볍게 인코딩/디코딩
- **(c) 패킷 손실 은닉(PLC)** — BT 패킷이 한둘 빠져도 끊김을 자연스럽게 메움

→ 무선 이어버드에 최적화된 코덱이라는 게 이 세 가지에서 드러난다.

> 출처: [Bluetooth.com LC3 technical overview](https://www.bluetooth.com/blog/a-technical-overview-of-lc3/) · [LC3 codec explained (besttechradar)](https://besttechradar.com/what-is-lc3-codec/) · [LC3 (Grokipedia)](https://grokipedia.com/page/LC3_(codec))

---

### VAD — 발화 구간 감지(Voice Activity Detection) 자세히 `firmware.html §5 / 웹 검증 2026-05-30`

**우리 용도 = 트리거 이후 '발화 끝' 감지(엔드포인팅)**
시작은 **트리거**(롱프레스 2초 → ARMED + 띵)가 잡는다. 사용자가 질문을 말하기 시작하면, VAD는 **언제 말이 끝났는지**를 잡아 **Track B 녹음을 자동 종료**한다 — 사용자가 "끝" 신호를 따로 줄 필요가 없게.
- **상시 동작 아님**: 트리거~최대 8초의 **짧은 녹음 창 동안만** 돈다 → 상시 저전력 부담이 작아 약간 무거운 방식도 허용.
- 규칙: 발화 후 **침묵 800ms 지속 → 종료**, 최소 500ms(짧은 오검출 방지)·최대 8초(상한).

**핵심 난점 — 녹음 중에도 다른 소리가 난다**: ① 이어폰 **내부 출력**(음악·영상)이 계속 재생 중일 수 있고 ② **외부**(카페 등) 소음이 있다. **순수 마이크 에너지 VAD는 이 소리들을 "아직 말하는 중"으로 오인** → 8초 상한까지 못 끊는 사고. → **착용자 발화만 보는 방식이 필요.**

**방식 분류 (가벼움 → 무거움)**

| 방식 | 원리 | 연산량 | 잡음 강인성 | 정확도 | 버즈 탑재 |
|---|---|---|---|---|---|
| **에너지(RMS) 임계값** | 프레임 음량 > 문턱 = 음성 | 극소 | 약함(조용할 때만) | 낮음 | ✅ 항상 |
| **+ ZCR(영교차율)** | 부호 바뀌는 횟수로 유/무성음 구분 | 극소 | 보통 | 낮음~중 | ✅ 항상 |
| **WebRTC VAD (GMM)** | 서브밴드 에너지 + 가우시안 혼합모델, 공격성 0~3 | 작음(고정소수점) | 중 | 중 | ✅ MCU OK (수십억 사용자 표준) |
| **Picovoice Cobra (DNN)** | 경량 딥러닝 | 중 | 강함 | 높음(WebRTC·Silero 능가) | ✅ Cortex-M4 데모 검증(상용 라이선스) |
| **RNNoise (GRU)** | RNN 잡음억제 + VAD 동시 | ~40 MFLOPS | 강함(+노이즈 제거 보너스) | 높음 | ⚠️ 오디오 DSP/고정소수점이면 가능, Cortex-M엔 무거움 |
| **Silero VAD (DNN)** | 신경망 8/16kHz, 30ms를 ~1ms | ~1.8MB + ONNX | 강함 | 매우 높음 | ⚠️ NPU 있는 고급 SoC면 OK, 저가 MCU엔 무거움 → 폰/서버용 |
| **대형 신경망(CNN+LSTM 등)** | — | 큼 | 최상 | 최상 | ❌ 서버 |

> ※ 우리 용도(트리거 후 짧은 창)에선 **상시 전력이 덜 중요** → 정확도 높은 방식을 짧게 켜는 게 유리.

**버즈의 정답 — 가속도계/골전도 VAD를 '엔드포인팅'에** ⭐
- 가속도계(음성 픽업 유닛 VPU)는 **착용자 턱·두개골 진동만** 본다 → **내부 출력·외부 소음에 면역**. 음악이 깔려 있고 카페가 시끄러워도 **"말 끝"을 정확히** 잡는다. (= §핵심 난점을 센서로 우회)
- 트리거 이후 **창에서만 켜면** 되니 전력도 문제없음.
- **실제 부품/제품**: Vesper **VA1200** 피에조 보이스 가속도계(주변음 면역) · **AirPods Conversation Awareness**(H2칩 가속도계로 발화 감지) · **삼성 갤럭시 버즈 "Voice Detect"**(양쪽 버즈 음성감지 센서) → **버즈가 이미 쓰는 기술 재활용.**

**확정 — 가속도계/골전도 VAD 단일** (보조·노이즈억제 없이 이거 하나로)
- 착용자 턱·두개골 진동만 보므로 **재생음·주변소음 면역** → 트리거 후 발화 끝(**침묵 800ms**)을 정확히 잡아 종료.
- 트리거가 연 **창에서만** 켜면 됨. 소프트웨어 VAD·노이즈 억제 같은 보조는 **안 씀** — 가속도계 하나로 충분.
- (위 방식 분류표는 "왜 가속도계냐"의 참고용 — 소프트웨어 VAD들은 재생음·주변소음에 약해 우리 상황엔 부적합.)

**실례 라이브러리**: WebRTC VAD(VoIP 표준) · Silero VAD(Whisper 등 ASR 프런트엔드, 1.8MB) · RNNoise(Discord 노이즈 억제) · Picovoice Cobra(Cortex-M 온디바이스).

> **출처 — 가속도계 발화 감지가 이미 양산 중이라는 근거**
> - **삼성 갤럭시 버즈 "Voice Detect"**: 양쪽 버즈의 **음성감지 센서로 착용자 발화를 감지** → 주변음 모드 전환 ([Samsung 지원](https://www.samsung.com/us/support/answer/ANS10001321/)).
> - **부품**: Vesper **VA1200** — TWS용 피에조 **보이스 가속도계**, 골전도로 착용자 목소리만 픽업, **주변음(바람·음악·지하철·군중) 면역** ([audioXpress](https://audioxpress.com/news/vesper-introduces-analog-piezoelectric-voice-accelerometer-for-true-wireless-earbuds)).
> - **논문**: In-Ear-Voice — 인이어 **골전도 마이크**로 milli-watt급 음성 향상 ([arXiv 2309.02393](https://arxiv.org/pdf/2309.02393)).
> ※ 정직: **"가속도계로 발화 감지"는 양산 중**(AirPods·갤럭시 버즈), **"골전도 마이크"는 부품/특허 수준**(Vesper 등) — 둘을 구분해 표기.

---

### 블루투스는 어떻게 정보를 주고받나 (기초) `PAN 이해를 위한 사전 정리`

PAN/BNEP를 이해하려면 그 아래 블루투스가 어떻게 도는지부터.

**① 두 종류의 블루투스 (라디오가 다름)**
- **BT Classic (BR/EDR)**: 고대역 연결. 음악(A2DP)·통화(HFP)·**PAN(네트워킹)** 등. 실효 ~1~3 Mbps, 전력 큼.
- **BLE (Bluetooth Low Energy)**: 저전력·소량 데이터. 센서·웨어러블·**LE Audio**(LC3).
- 둘 다 2.4GHz를 쓰지만 **사실상 다른 프로토콜**. 버즈는 보통 둘 다 지원.

**② 라디오 — 전파를 쏘고 받는 하드웨어**
실제로 전파를 내보내고 받는 부품은 **딱 두 개**다 (별도 '랜카드'가 아니라 **SoC 안 무선회로 + 작은 안테나**):
- **① RF 트랜시버** — 디지털 0/1을 **2.4GHz 전파로 변환(송신)**하고, 들어온 전파를 다시 디지털로(수신). **블루투스 SoC 안에 내장**. (+ **PA**=송신 증폭, **LNA**=약한 수신 신호 살림)
- **② 안테나** — 전자기파를 **공기 중으로 방사·수신**하는 금속. 버즈에선 **작은 칩 안테나나 PCB/하우징에 새긴 패턴**.
```
[디지털 데이터] → RF 트랜시버 → 안테나 → 📡 2.4GHz 전파
   (SoC 안)         (변조·증폭)   (방사)
```

**2.4GHz를 잘게 쪼개 뛰어다님 (주파수 호핑)**
- 위 회로·안테나는 **2.4GHz 전용** 설계. 그 대역을 채널로 분할(Classic 79개·1MHz / BLE 40개·2MHz).
- **적응형 주파수 호핑(AFH)**: 초당 ~1600번 채널을 바꿔 뜀 → Wi-Fi·전자레인지 간섭·도청 회피. "한 채널에 안 머문다."

**③ 연결 구조 — 센트럴 1 + 페리페럴 N (피코넷)**
- 한쪽이 **센트럴**(폰/호스트), 다른 쪽이 **페리페럴**(버즈). 센트럴이 타이밍을 잡고 동기. (옛 용어 master/slave)
- 연결 전 **페어링·본딩**(키 교환) → 이후 **링크 레벨 AES 암호화**.

**④ 프로토콜 스택 — 층층이 쌓아 "의미"를 부여**
```
[프로파일]  A2DP(음악) · HFP(통화) · PAN/BNEP(네트워크) · GATT(BLE 데이터) · LE Audio
   ▲
[L2CAP]    논리 채널로 멀티플렉싱 (PSM/CID로 여러 통로 구분)
   ▲
[링크/베이스밴드(LL)]  연결·호핑·재전송·암호화
   ▲
[라디오 PHY]  2.4GHz 송수신
```
- **핵심: 블루투스는 "바이트 통로(L2CAP 채널)"만 깔아주고, 그 바이트가 무슨 뜻인지는 '프로파일'이 정한다.**
  - A2DP면 그 바이트 = 오디오, HFP면 통화, **PAN/BNEP면 = 네트워크 패킷(IP).**
- 그래서 같은 버즈가 **여러 프로파일을 동시에** 운용: 음악(A2DP/LE Audio) + 데이터(PAN) 병행.

**⑤ 정보 주고받는 한 사이클**
1. 광고/검색(advertising/inquiry) → 발견
2. 페어링·본딩(키 교환) → 암호화 설정
3. 연결 수립(센트럴↔페리페럴) → L2CAP 채널 개설
4. 프로파일이 그 채널 위에서 데이터에 의미 부여(음악/통화/IP)
5. 끊김 대비 재전송·호핑은 링크 레벨이 자동 처리

→ **다음(Layer 2)**: BudsAI는 이 중 **PAN 프로파일**을 골라 L2CAP 위에서 **BNEP로 IP 패킷을 실어** 호스트를 거쳐 인터넷에 나간다. 즉 "버즈가 블루투스로 인터넷에 테더링"하는 것.

---

### Layer 2 — 전송: Bluetooth PAN / BNEP `bnep-slide.html / host-bridge.html`

- **프로파일**: Bluetooth PAN (Personal Area Network)
  - 버즈 = **PANU** (PAN User, UUID `0x1115`)
  - 호스트 = **NAP** (Network Access Point, UUID `0x1116`)
- **왜 커스텀 프로파일이 아니라 PAN인가**: BT 1.1(2003)부터 표준, **모든 데스크톱/모바일 OS에 빌트인** → 앱·드라이버·스토어 심사 0
- **BNEP** (Bluetooth Network Encapsulation Protocol)
  - L2CAP PSM `0x000F`
  - 압축 타입 `0x02` (12B MAC 생략, 재조립 시 복원)
  - NetTypeFilter: IPv4 + ARP only (옵션)
  - MTU: L2CAP CoC 협상, 512B~1KB/frame (예: 1KB MTU → 130KB 요청 ≈ 130 frames)
- **BT Classic** 주 경로(대역폭↑) / BLE IPSP 대안(저전력, 지원 제한)

**Fallback 4단계** (host-bridge)
- Tier 1: BT PAN (기본, 90%+ 기기) — zero-install
- Tier 2: Web Bluetooth 브릿지 (QR→브라우저 페어링, iOS Safari 제외)
- Tier 3: 범용 BudsBridge 앱 (iOS·PAN 미지원 호스트용, 단일 앱)
- Tier 4: 버즈 내장 eSIM (NB-IoT/LTE-M, 호스트 불요, 프리미엄)

---

### Layer 2b — 호스트의 두 얼굴 `host-bridge.html / diagram-combined.svg`

호스트는 **누구냐에 따라 역할이 둘**이다 (전체 대비표는 §0.5):

**🔴 비-Galaxy 호스트 = Dumb Pipe**
- 호스트가 하는 일 **딱 4가지**: BNEP L2 프레임 송수신 / 버즈에 DHCP IP 할당 / NAT·라우팅(MASQUERADE) / DNS 포워딩
- 호스트가 **못 보는 것**: TLS 핸드셰이크, 세션 메시지, Opus 오디오, 인증서 — 전부 mTLS E2E 암호화
- 인증은 버즈 기기 인증서(mTLS, SoC 키스토어), TTS는 서버가 생성

**🔵 Galaxy 폰 = 스마트 프록시**
- 폰이 **능동 참여**: 요청 local DB 저장(SQLite), JWT로 서버 인증, 응답 session_id 매핑, action item 후속처리, **TTS 폰에서 생성(VITS)**
- 폰은 자사 기기라 페이로드를 봐도 됨 → 생태계 통합·on-device TTS 이득

**공통 — OS 빌트인 테더링**
- 한 토글: Windows/macOS/Linux(BlueZ)/Android/ChromeOS 완전 지원, iOS 제한적(→ Tier 2/3 fallback)
- 버즈 측 펌웨어 비용: BNEP ~8KB + lwIP ~30~60KB + mbedTLS/WolfSSL ~80~120KB + DNS/DHCP ~10KB + HTTP ~20~40KB = **~150~250KB** (현대 BT SoC 플래시로 수용 가능)

---

### Layer 2c — 블루투스 테더링 캡슐화 흐름도 (버즈 → 호스트 → 서버 → 복귀) `dumb pipe 경로`

핵심: 내용물(SBAP)을 **여러 겹으로 감싸** 보내고, 호스트는 **딱 필요한 겉껍질만 까서 NAT 찍고** 다시 감싸 인터넷으로 보낸다. **TLS 속 알맹이는 안 깐다.**

**① 버즈 — 양파처럼 감싼다 (캡슐화)**
```
[ SBAP: 호출명령 + A-int + A-ext + B ]   ← 알맹이(앱 페이로드)
 └ TLS 1.3 (mTLS)  암호화                 ← 여기부터 호스트가 못 봄 🔒
   └ TCP / IP   (출발 192.168.44.2 → 서버IP)
     └ Ethernet 프레임
       └ BNEP   (L2CAP PSM 0x000F 위)
         └ Bluetooth (L2CAP → 베이스밴드 → 2.4GHz 라디오)  📡
```
- 연결 시 호스트가 버즈에 **사설 IP를 DHCP로 발급**(예 192.168.44.2). 버즈는 호스트 PAN 서브넷의 한 기기가 됨.

**② 호스트(NAP) — 까는 만큼만 깐다 + NAT**
```
📡 BT 수신 → L2CAP → BNEP 해제 → Ethernet/IP 프레임 복원
   │
   ├─ IP/TCP 헤더만 열람·수정  =  NAT (MASQUERADE)
   │     출발지  192.168.44.2:51000  →  호스트 공인IP:62000
   │     NAT 표에 매핑 기록(conntrack):  [192.168.44.2:51000 ↔ 공인IP:62000]
   │
   ├─ TLS 속 SBAP(알맹이)는 그대로 = 암호문  🔒 안 깜 → dumb pipe
   │
   └─ 호스트의 인터넷 링크(Wi-Fi/LTE/Ethernet)에 IP 패킷 재포장 → 송신
```
→ **호스트가 까는 깊이 = IP/TCP 헤더(L3/L4)까지만.** 라우팅·NAT에 필요한 봉투 주소만 보고, **TLS 안 내용은 절대 안 본다.**

**③ 호스트 → 서버**
```
[ ...TLS(SBAP)... 그대로 ] + 새 IP/TCP 헤더(출발=호스트 공인IP)
   → 인터넷 라우팅 → 서버
서버: mTLS 종단에서 복호 → SBAP 파싱(명령+3스트림) → STT·의도분류·핸들러 → 응답 생성
```

**④ 복귀 — 거꾸로 되돌린다 (역캡슐화·NAT 역변환)**
```
서버 응답 [ TLS(TTS / text / action) ]  →  호스트 공인IP:62000 도착
호스트 NAT 역조회:  목적지 공인IP:62000  →  192.168.44.2:51000  (②의 표 참조)
   → IP 패킷을 Ethernet → BNEP → L2CAP → BT 로 다시 감쌈 📡
   → 버즈로 전송
버즈: BT → BNEP 해제 → TCP/IP → TLS 복호 → 응답 재생/처리 🔊
```

**한눈 요약**
```
버즈 [SBAP|TLS|IP|BNEP|BT] ─📡─▶ 호스트 (BNEP 벗김 → IP/TCP만 보고 NAT → 인터넷 봉투 다시 씌움) ─▶ 서버
버즈 ◀─📡─ 호스트 (NAT 역변환 → BNEP 다시 씌움) ◀─ 서버 [응답|TLS|IP]
                         ▲ 알맹이(TLS 속)는 양쪽 호스트 통과 내내 암호문 — 절대 안 까임
```
- **왜 좋은가**: 이 과정 전부가 **OS 빌트인 테더링/인터넷 공유** 기능 그대로 → 호스트에 앱·드라이버 설치 0. 호스트는 봉투만 바꿔 끼우고 **알맹이는 못 본다** → 호스트 무관 + E2E 보안 동시 성립.

---

#### 🟢 쉬운 버전 (용어 정리 — 택배 비유)

**용어 한 줄 정리**

| 용어 | 쉬운 뜻 |
|---|---|
| **내/외부 소리 + 사용자 발화** | 보낼 **내용물**(방금 듣던 소리 + 내가 한 말) |
| **IP** | 인터넷에서 기기를 찾는 **주소 체계**(인터넷 집주소) |
| **출발지 주소** | 보내는 쪽 = **버즈** 주소 |
| **도착지 주소** | 받는 쪽 = **서버** 주소 |
| **Bluetooth** | 버즈 ↔ 폰/노트북 사이 **가까운 거리 무선 전달**(손배달) |
| **BNEP** | 블루투스로 **인터넷 택배(IP 패킷)를 실어 나르는 포장 규격** |

**택배처럼 보면**
```
[ 내/외부 소리 + 발화 ]   ← 보낼 내용물
  봉투에 주소 적기:  출발지 = 버즈 / 도착지 = 서버   (= IP 주소)
  BNEP 포장으로 블루투스에 싣기
  Bluetooth로 폰/노트북에 전달  📦→
        │
   폰/노트북:  BNEP 포장 벗김  →  출발지 주소를 '자기 주소'로 고쳐 적고
              (돌아올 때 누구한테 줄지 표에 메모 = NAT)  →  인터넷으로 서버에 전달
        │
   서버:  봉투 열어 내용물 처리  →  답장 작성
        │
   폰/노트북:  답장의 도착지를 다시 '버즈 주소'로 고쳐 적고  →  블루투스로 버즈에 전달
        │
   버즈:  답장 받아 재생  🔊
```

- 한 줄: **버즈가 "내용물 + 출발지/도착지 주소"를 BNEP·블루투스로 포장해 폰에 주면, 폰은 포장만 갈아 끼워 서버에 보내고, 답장도 같은 방식으로 되돌려준다.**
- 폰은 **봉투(주소)만 보고 갈아 끼울 뿐, 내용물은 안 열어본다.**

---

### Layer 3 — 패킷 포맷 `packet.html`

**세션 메시지 = 헤더(16B) + 메타데이터(TLV) + 오디오 페이로드 + 푸터(CRC32)**

헤더 16B 고정:
- Magic `42 55 44 53` ("BUDS") · Version 1B · MsgType 1B(`01`req/`02`resp/`03`ack/`04`err)
- Flags 2B (bit0 압축 / bit1 암호 / bit2 TrackA-int / bit3 TrackA-ext / bit4 TrackB / bit5 연속성)
- Session ID 4B (요청마다 랜덤) · Total Length 4B

메타데이터 (TLV) 주요 필드: DeviceID(16B) · Timestamp(8B) · TriggerType · **스트림별(A-int / A-ext / B) Codec(LC3)·SampleRate·Duration·Offset·Length** · BatteryLevel · FirmwareVersion · Locale · **`0x11` PrevSessionID(4B) = 멀티턴 연속성 핵심**

사이즈 견적 (한 요청, LC3 기준, **Track A 내부/외부 분리**):
- A-int ~90KB + A-ext ~90KB + Track B ~15KB + 헤더/메타/푸터 ~100B = **~200KB** (전부 LC3 24kbps)
- BT 전송 프레임: SOF `0xA5` + Seq + Total + Flags + Payload(240~1000B) + CRC16, NAK 재전송 최대 3회. 1KB MTU 기준 ~200 frames.

---

### Layer 3+ — 블루투스 안 내용물 = Samsung 내부 프로토콜 (SBAP) `호출 명령 + 3스트림`

위 패킷을 **삼성 사설(내부) 애플리케이션 프로토콜**로 공식화한다 — 가칭 **SBAP (Samsung BudsAI Protocol)**.
중요: SBAP는 **표준 BT PAN/IP/mTLS 위에 올라가는 *페이로드 규약***이다. 호스트는 여전히 바이트만 나르는 dumb pipe → **호스트 무관 유지**. 한 줄로: **"전송은 표준, 의미(내용물)는 사설."**

**한 메시지 = 헤더 + 호출 명령 + 메타 + 3스트림 + 푸터**
```
┌ 헤더(16B) ── Magic"BUDS" · Ver · MsgType · Flags(A-int/A-ext/B/연속성) · SessionID · TotalLen
├ 호출 명령(Command) ── ★ "BudsAI를 호출" + 어떻게 처리할지
│    · Cmd(1B)      : INVOKE / FOLLOWUP / CANCEL / PING
│    · RespMode(1B) : bit0 audio(TTS) · bit1 text · bit2 action(생태계 라우팅)
│    · EcoHint(1B)  : Galaxy 라우팅 힌트(Spotify/Notes/Calendar…) (선택)
├ 메타(TLV) ── DeviceID · Timestamp · TriggerType · Locale · 스트림별(A-int/A-ext/B) Codec=LC3·SR·Dur·Offset·Len · Battery · FwVer · PrevSessionID
├ 페이로드 ── [A-int LC3][A-ext LC3][B LC3]   (~90 / ~90 / ~15 KB)
└ 푸터 ── CRC32 (+ 옵션 AEAD 태그/서명)
```

**핵심: "BudsAI 호출 명령 + 3개 컨텍스트 스트림(내부·외부·발화)"을 한 메시지로 원자적으로 묶는다.** 표준 오디오 프로파일(A2DP=재생 1스트림, HFP=통화)엔 "듣던 내부음 + 주변 외부음 + 내 발화 + 처리 명령"을 한 번에 담는 시맨틱이 **아예 없다** → 그래서 사설 프로토콜이 필요.

> **의도 분류는 100% 서버** — 버즈는 의도를 추측하지 않는다(그래서 명령에 IntentHint 없음). 버즈는 "**호출 + 3스트림**"만 보내고, 무슨 의도인지는 서버가 STT→의도분류기로 판단. 장점: ① **버즈 펌웨어 단순**(의도 로직 0) ② 의도 추가·변경 시 **서버만 업데이트 — 전 기기 OTA 불필요** ③ 단일 서버 모델로 **일관·언제든 개선**.

**삼성 내부 프로토콜로 만들면 좋은 점**
1. **3트랙+명령 동시 운반** — 표준론 불가능한 "내부·외부·발화 3스트림 + 호출 의도"를 한 패킷에. **제품의 본질(컨텍스트 동봉)이 프로토콜에 직접 박힘.**
2. **명령과 컨텍스트의 원자적 결합** — "호출"과 그 근거 오디오가 한 메시지 → 분실·순서꼬임·레이스 없음.
3. **자사 생태계 메타 자유** — EcoHint·x-galaxy-intent·Knox 보안 필드 등 삼성 전용 필드를 자유롭게 추가/진화. **SIG 표준화 절차 불필요 → 빠른 이터레이션.**
4. **효율 최적화** — 우리 용도에 맞춰 헤더 최소화, **LC3 프레임 그대로 운반(트랜스코딩 0)**, 표준의 불필요 오버헤드 제거.
5. **보안 1급 통합** — mTLS 종단·서명·AEAD 태그를 프로토콜에 내장 → 호스트 불투명(dumb pipe)과 자연 결합.
6. **종단 일관성·QA 단순** — 버즈 칩 ↔ 서버 ↔ Galaxy 앱이 **같은 사설 규약** 공유 → end-to-end 디버깅·버전관리 일원화.
7. **복제 난이도 = 해자/IP** — 자사 칩·앱·서버가 공유하는 사설 규약 → 경쟁사가 통합 경험을 쉽게 못 베낌 + 특허·IP 자산.

> ⚖️ **대가(정직)**: 비표준이라 **타사 기기와 직접 상호운용 불가**. 단, 전송은 표준 IP/PAN이라 **호스트 무관은 유지**(호스트는 통로일 뿐). 자체 **문서화·버전·유지보수 부담**은 짊어진다. → "**전송은 표준, 내용물은 사설**"이 균형점.

---

### Layer 4 — 서버 AI 파이프라인 `server-api.html`

**엔드포인트**: `WS /v1/stream`(메인 스트리밍) · `POST /v1/sessions`(폴백) · `POST /v1/sessions/{id}/followup`(연속성) · `POST /v1/auth/token`(앱 JWT) · `DELETE /v1/user/data`(GDPR) 등

**파이프라인**: Track B decode(~20ms) → STT(~150~250ms, Whisper급) → **의도분류(~30~80ms)** → Track A decode(병렬 ~100ms) → 핸들러(~200~1500ms) → LLM 첫토큰(~300ms) → TTS 첫청크(~200ms) → WS 바이너리 스트리밍(클라 200ms 지터버퍼)

**의도 분류기 (8개 핵심 + 보조)**:
`song_id` 음악식별 · `transcribe` 받아쓰기 · `summarize` 요약 · `translate` 번역 · `define` 단어뜻 · `classify` 환경음분류 · `recommend` 추천 · `search` 검색(RAG) — + 보조 `followup` / `general` / `unclear`(신뢰도<0.4 재질문)

**TTS 출력**: **LC3 16/24kHz mono ~24kbps** (기존 Opus에서 변경 — 버즈가 LC3로 디코드해 바로 재생, 코덱 일원화), 첫 6~8토큰에서 생성 시작 → WS 바이너리 청크 스트리밍. ※ Galaxy 경로 폰-side TTS도 LC3로 출력 (불일치 3 확정에 따라).

**세션 저장**: Redis(30분 TTL: 메타/지문/응답텍스트/의도) + PostgreSQL(옵션 영구) — PrevSessionID로 후속 질문 연결

> ⚠️ **불일치 2**: "8개 의도"라고 부르지만 원문 핸들러 목록은 8개 핵심(song_id/transcribe/summarize/translate/define/classify/recommend/search) + 보조 3개(followup/general/unclear). 장표에서 "8개"로 셀지 "8+3"으로 셀지 통일 필요.

---

### 보안 — mTLS 종단을 버즈에 `proposal / packet / server-api`

> 이 섹션은 슬라이드 1~2장 분량. **TLS가 뭔지 → mTLS가 뭐가 다른지 → 어떻게 종단간 보안이 되는지 → 사용자가 얻는 것** 순서로 설명.

#### (1) TLS란? — HTTPS를 떠받치는 그 기술

TLS(Transport Layer Security)는 **인터넷에서 두 지점이 안전하게 대화하게 해주는 표준 프로토콜**(웹 주소창의 자물쇠 🔒 = HTTPS = HTTP + TLS). 딱 **세 가지**를 보장한다:

| 보장 | 의미 | 깨지면 |
|------|------|--------|
| **① 기밀성 (Confidentiality)** | 내용을 암호화 → 중간에서 엿봐도 암호문만 보임 | 도청·감청 |
| **② 무결성 (Integrity)** | 한 비트라도 바꾸면 검사 실패 → 거부 (AEAD) | 변조·주입 |
| **③ 인증 (Authentication)** | 인증서로 "상대가 진짜인지" 증명 | 가짜 서버·피싱 |

**일반 TLS의 한계**: 인증이 **한쪽 방향**이다. 브라우저가 서버 인증서만 검증한다(서버가 진짜인지). **클라이언트(나)가 누구인지는 TLS가 증명 안 함** → 그래서 따로 비밀번호·로그인·토큰이 필요하다.

#### (2) mTLS란? — 양쪽 다 신분증을 내미는 TLS

**mTLS(mutual TLS, 상호 인증 TLS)** = 서버뿐 아니라 **클라이언트도 자기 인증서를 제시**해서 **양방향으로 서로를 암호학적으로 검증**하는 TLS.

- 버즈는 **자기만의 디바이스 인증서(X.509) + 개인키**를 가진다 — 출고 시 발급, **칩(SoC) 안의 하드웨어 보호 키스토어에 저장, 밖으로 나가는 경로 없음** (어떻게 가능한지는 (2.5)~(2.7)).
- 서버는 "**아무 인증서나 말고, 우리가 발급한 디바이스 인증서를 가진 버즈만**" 받도록 설정.
- 결과: **비밀번호·앱 로그인 0**으로도 서버는 "이건 진짜 디바이스 #ABC다", 버즈는 "이건 진짜 BudsAI 서버다"를 **수학적으로 확신**.

#### (2.5) 결정 — 독립 SE 칩이 아니라 'SoC 통합 보안'으로 간다

**✅ 채택: 개인키를 메인 BT SoC 안의 하드웨어 보안영역(TrustZone-M + 키관리유닛 KMU + 시큐어부트)에 보관한다. 별도 SE 칩은 넣지 않는다.**

**왜 SoC인가 (위협 모델로 판단)**
- 결제·여권급 **독립 SE(EAL6, anti-decap)**는 *돈·신분증*을 자금력 있는 물리 공격자로부터 지키는 용도 → 우리 자산은 "음성 + API 접근"이라 **과함(배보다 배꼽).**
- 버즈의 실제 위협: ① 못 믿는 호스트의 도청 → **키 위치와 무관하게 mTLS로 이미 해결** ② 캐주얼한 키 추출 → **SoC 내장 보안이면 good enough.**
- 결정타: **버즈엔 이미 SoC(두뇌 칩)가 있다** → 보안 내장 세대로 고르면 **추가 칩 0**.

**현 상태(실측)와 갭**
- 현재 갤럭시 버즈(Pro)는 분해 기준 **Broadcom BCM43015 오디오 SoC + 외부 NOR 플래시** → 펌웨어가 외부에 평문, 덤프 가능 = **보안 약함**. (Buds4 Pro 단독 분해도는 비공개)
- → 이 설계의 전제 = **보안 내장 BT SoC(TrustZone-M·RoT·KMU)로 세대 교체.** 스마트링·헬스밴드(Nordic)·이어버드칩(Qualcomm QCC)이 이미 그 부류 → **업계 표준 따라가기**.

**키 수명주기 (요약)**
- **기기마다 고유**: 제조 시 각 버즈 SoC 안에서 **고유 키쌍을 온칩 생성** → 분실 시 **그 기기 인증서 1개만 폐기**(`CERT_REVOKED`). 서버엔 **공개키/인증서만** 등록.
- **유출**: 개인키는 키스토어 밖으로 **읽어내는 경로가 없음**(공유되는 건 공개키뿐). "수학적 절대 불가"는 아니어도(랩 수준 물리 공격) **뚫어도 그 기기 하나 + 즉시 폐기** → 경제적으로 무의미.

> 🔎 **0은 안 됨**: 2025년 **Airoha 기반 TWS들이 인증 없이 BT로 탈취**된 취약점 → 하드웨어 신뢰근원+시큐어부트는 **필수**. 단 *독립 SE일 필요는 없다.*
> 🧩 **더 줄이려면 (옵션)**: ① **SRAM PUF** — 키를 저장조차 안 하고 실리콘 편차로 매번 생성(덤프 대상 0) ② Galaxy 경로는 폰 **Knox**에 신뢰앵커 위임 ③ 독립 SE는 엔터프라이즈/고보안 SKU 옵션.

#### (2.6) SoC 통합 보안이 실제로 하는 일 (4블록)

폰 SE가 하던 일을 **메인 BT SoC 한 칩 안에서 하드웨어로** 구현한다.

**① ARM TrustZone-M (Cortex-M33/M23)** — 칩을 하드웨어로 **Secure World / Non-secure World 둘로 분할**. 키·암호 연산은 Secure World에만, 일반 펌웨어(Non-secure)는 **읽지 못하고 함수 호출만** 가능.

**② 시큐어 부트 / 신뢰 근원(RoT)** — 부팅 때 **ROM/OTP의 불변 루트키로 펌웨어 서명 검증** → 정품 펌웨어만 실행. (현행 '외부 NOR 평문 펌웨어' 약점을 메움)

**③ 암호 엔진 + 키 관리 유닛(KMU)** — HW AES/ECC/SHA/TRNG. 키는 **"쓸 수는 있지만 읽을 수는 없는"** 영역에 저장, 펌웨어는 키 핸들만 참조:
```
펌웨어 ──"키 #3으로 이 값 서명"──▶ [암호엔진: KMU의 키로 서명]
펌웨어 ◀──────"서명 결과만"────────  (키 원본 노출 0)
```

**④ TRNG·OTP·시큐어 디버그** — 키쌍 온칩 생성, 루트 비밀은 OTP read-lock, 양산품은 **JTAG/SWD 디버그 잠금**으로 메모리 덤프 차단.

**웨어러블 실사용 근거**:
- **Nordic nRF5340**(듀얼 Cortex-M33 + TrustZone + CryptoCell-312 + KMU + 시큐어부트) — '**advanced wearables + LE Audio**'용. 실제 **스마트링(Ultrahuman Ring Air)·헬스 웨어러블(NNOXX One)** 양산 탑재 → **버즈보다 작은 반지도** 이 보안 SoC 구동.
- **Qualcomm QCC**(이어버드 SoC) — **RoT CPU + 시큐어부트 + KMU + TEE** 내장 → 이어버드 칩 업계가 이미 가는 방향.

**정직한 한계 (vs 독립 SE)**: TrustZone-M은 **논리 격리+시큐어부트+디버그잠금**이라 SW·펌웨어 공격·캐주얼 추출엔 강하지만, 메인 CPU와 **같은 다이·전원**이라 디캡·고급 부채널 같은 **정교한 물리 공격 저항은 EAL6 독립 SE보다 약함**. 우리 위협 모델엔 합리적이고, 인증도 SE의 Common Criteria 대신 **ARM PSA Certified**(TF-M 기반)로 대응.

#### (2.7) "SoC에 넣는다 = 그냥 펌웨어에 코드 추가하는 거 아냐?" → 아니다

좋은 질문 — **여기가 핵심**이다. "SoC에 넣는다"를 "펌웨어 코드에 키를 박는다"로 들으면 안 된다. **'코드(소프트웨어)'와 '하드웨어 보안 블록'은 다른 층**이고, 실제로 **3층**으로 나뉜다:

| 층 | 정체 | 키와의 관계 |
|---|---|---|
| ① **일반 펌웨어** (Non-secure) | 평범한 코드 (외부 NOR에도 둠, 덤프 가능) | **키 못 봄.** 여기 키 넣으면 ❌ — *네가 걱정한 바로 그 경우* |
| ② **보안 펌웨어** (Secure World, TF-M) | 코드이긴 하나 **TrustZone이 하드웨어로 격리**한 영역에서만 실행, ①이 못 들여다봄 | 키를 **다루는 로직**(서명 요청 처리) |
| ③ **하드웨어 블록** (KMU·암호엔진·OTP·시큐어부트 ROM) | **코드가 아니라 실리콘** | **키 원본이 여기 산다.** CPU조차 '읽기'는 못 하고 '쓰기(서명)'만 시킴 |

→ **키는 코드가 아니라 하드웨어(③)에**, 접근은 **격리된 보안 영역(②)에서만**, **일반 펌웨어(①)는 절대 못 건드린다.** 코드가 추가되긴 하나(②) 일반 펌웨어 블롭과 **하드웨어로 분리**돼 있고, **키 그 자체는 코드가 아니다.**

비유로:
- ❌ **펌웨어에 키 박기** = 금고 비밀번호를 **포스트잇에 적어 책상에 붙임**.
- ✅ **SoC 보안** = 비번을 **금고 안 다이얼 기계(하드웨어)에 박고**, 금고는 **잠긴 별도 방(TrustZone)**에, 방문은 **하드웨어 자물쇠(시큐어부트)**로 잠금 → 누구도 비번을 *읽지* 못하고 "잠가/열어"만 시킨다.
- 자판기와 같다: **버튼 눌러 쓸 수는 있어도(서명)** 안의 **현금통(키)은 못 꺼낸다** — 현금통은 포스트잇이 아니라 철제 박스(실리콘).

#### (3) 핸드셰이크 — 어떻게 종단간 보안이 성립하나 (TLS 1.3 mTLS)

```
버즈 ──①ClientHello + 키재료(ECDHE)──▶ 서버
버즈 ◀──②서버 인증서 + 키재료 + "너 인증서도 줘"── 서버
버즈 ──③서버 인증서 검증(신뢰된 CA 서명? 진짜 서버?) → 가짜면 여기서 중단──
버즈 ──④디바이스 인증서 + 개인키 서명 제시──▶ 서버
서버 ──⑤버즈 인증서 검증 → 위조 기기면 거부──
양쪽 ──⑥공유 비밀 도출 → 세션키 생성 → 이후 모든 패킷 AEAD 암호화──
```

- ⑥의 키 교환은 **ECDHE** → **순방향 비밀성(forward secrecy)**: 나중에 키가 털려도 **과거 대화는 복호화 불가**.
- 이후 모든 페이로드는 **AEAD(ChaCha20-Poly1305 / AES-GCM)** 로 암호화+무결성 동시 보호.
- (옵션) packet 레벨에서 ChaCha20-Poly1305 추가 암호 1겹 더 가능 (Flags bit1).

**"종단간(End-to-End)"의 핵심**: 이 암호 세션의 **두 끝(키를 쥔 주체)은 버즈와 서버**다. 그 사이의 **호스트 OS·BNEP·인터넷 라우터·ISP는 전부 암호문만 나르는 "봉인된 행낭 배달부"** — 열쇠가 없어 내용을 못 보고, 뜯으면(변조) 무결성 검사에서 들킨다. 개인키가 **버즈 SoC 보안영역 밖으로 안 나가므로** 종단 자체가 하드웨어로 잠겨 있다.

> ⚠️ **경로 주의 (§0.5와 일관)**: 이 **버즈↔서버 완전 E2E는 🔴 비-Galaxy(dumb pipe) 경로**에 해당. 🔵 Galaxy 경로는 **신뢰된 자사 폰이 종단**(버즈↔폰 BT 암호 + 폰↔서버 TLS+JWT)이라 폰이 평문을 본다 — 단, 사용자 본인의 Knox 보안 기기라 위협 모델이 다름. 장표에선 "**못 믿는 남의 기기를 쓸 때(비-Galaxy)는 버즈가 직접 E2E, 내 폰일 때는 폰이 안전하게 중계**"로 설명.

#### (4) 솔직히 — 누가 진짜 이득을 보나

보안 기능은 **대개 사용자보다 운영자가 더 큰 이득**을 본다. 과장 없이 갈라서 정리한다.

**(4a) 사용자가 진짜 체감하는 것 — 솔직히 이 3개뿐**

| 상황 | 사용자 체감 이득 | 체감 강도 |
|---|---|---|
| 회사 노트북·렌터카·호텔 TV를 경유 | **아무 기기나 거리낌 없이 빌려 써도 내 대화·듣던 소리가 안 샌다** | ★★★ 행동이 바뀜 |
| 켜면 그냥 됨 | **비번·앱 로그인 0** (인증서가 자동 처리) — zero-install 편의 | ★★★ 매일 체감 |
| 버즈 분실·도난 | 잃어버려도 **그 기기만 원격 폐기** → 안심 | ★★ 사고 시 |

> 솔직 메모: **피싱/MITM 차단·요청 위조 방지·패킷 변조 방지**는 "**보이지 않는 보험**" — 평소 사용자는 체감 못 한다(사고가 안 나니까). 가치가 없는 게 아니라 **사용자가 느끼는 이득이 아니다** → 사용자 슬라이드에 욱여넣지 말고 아래 운영자 이득으로 옮기는 게 정직.

**(4b) 서버 운영자가 얻는 것 — 사실 이쪽이 더 크다 (쉽게 풀이)**

**① 봇·가짜 트래픽이 아예 문 앞에서 튕긴다 → AI 비용 절감**
보통 인터넷 서버는 주소만 알면 누구나 두드릴 수 있다. 그래서 봇이 떼로 가짜 요청을 쏘거나, 훔친 계정으로 무작위 로그인을 시도한다. **AI 서버는 요청 1건마다 GPU 값이 비싸서 쓰레기 트래픽이 곧 돈**이다.
→ mTLS는 "**우리 공장에서 인증서를 심은 진짜 버즈**"만 통과시킨다. 인증서 없는 요청은 **핸드셰이크 단계에서 즉시 거부** — 서버 안으로 들어오지도 못한다.
→ **느끼는 것**: 봇 걱정 없이 잠잠하고, **AI 청구서가 딱 진짜 사용자 수만큼만** 나온다.

**② 누가 얼마나 썼는지 정확히 잡힌다 → 과금·이상탐지 정확**
무료/유료를 나누고 사용량 제한을 걸려면 "이 요청이 누구 건지"가 확실해야 한다. 비번·토큰은 공유되거나 새면 신원이 흐려진다.
→ 디바이스 인증서는 **기기당 하나, 위조 불가** → "이 요청 = 정확히 #ABC 버즈"가 100% 확실.
→ **느끼는 것**: 사용량이 깔끔히 집계돼 **과금·쿼터가 정확**하고, "평소 하루 10번 쓰던 기기가 갑자기 1만 번?" 같은 **이상 징후도 바로 잡힌다**.

**③ 중앙에 털릴 "비밀번호 창고"가 없다 → 유출 사고가 작아진다**
보통 시스템은 어딘가에 비번·API키를 모아 둔다. 그게 새면 **전 사용자 비번 리셋 + 뉴스 헤드라인**급 대형 사고다.
→ mTLS는 비밀(개인키)을 **각 버즈의 SoC 보안영역 안에만** 두고 밖으로 안 꺼낸다. **중앙에 모아 둔 비번 DB 자체가 없다.**
→ **느끼는 것**: 사고가 나도 피해가 **기기 한두 개로 한정**. 전사 비번 리셋·기자 대응 같은 악몽이 없다.

**④ 분실 처리는 클릭 한 번 → 나머지 사용자 무중단**
사용자가 버즈를 잃어버렸다고 신고했다. 보통이면 비번을 갈거나, 최악엔 전체 키를 교체해야 한다.
→ mTLS는 그 **기기 인증서 하나만 "폐기 목록"에 올리면 끝**(`CERT_REVOKED(401)`). 다음 접속부터 거부, 나머지 수백만 대는 영향 0.
→ **느끼는 것**: 분실 신고 처리 = **클릭 한 번**, 다른 사용자는 아무도 안 끊긴다.

**⑤ 음성 평문이 남의 기기에 안 남는다 → 규정·법무가 단순해진다**
사용자의 대화가 남의 노트북·렌터카를 거쳐 간다. 만약 그 호스트에 평문이 남으면 **개인정보 유출 책임이 우리에게** 오고, GDPR·개인정보보호법 감사 때 골치다.
→ E2E 암호라 **호스트는 암호문만** 보고, 평문은 **버즈와 서버에만** 존재한다.
→ **느끼는 것**: 감사관에게 "**우리는 제3자 기기에 평문을 절대 두지 않는다**"고 한 줄로 증명 — 법무·규정 검토가 단순해지고 **책임 범위가 좁아진다**.

**⑥ 들어온 데이터가 "보낸 그대로"임이 보장된다 → 데이터·로그가 안 오염된다**
중간에서 누가 데이터를 슬쩍 바꾸거나(변조), 같은 요청을 반복 재생(리플레이)해 시스템을 속일 수 있다.
→ AEAD 무결성 검사가 **한 비트만 바뀌어도 자동 폐기**한다.
→ **느끼는 것**: 로그·통계가 **오염 안 되고**, 조작된 요청으로 인한 장애·디버깅 삽질이 없다.

**한 줄 메시지**: mTLS+SoC 보안의 최대 수혜자는 솔직히 **운영자(어뷰징·비용·규정 방어)**. 사용자에게 진짜 남는 건 **"아무 기기나 써도 안 샌다 + 비번 없이 그냥 된다"** 둘. 그리고 이 구조 덕에 **호스트 무관(#02)이 안전하게 성립** — 보안과 차별점이 서로를 떠받친다.

#### (5) 부가 — Galaxy 시너지

- **Knox Vault**: 버즈 기기 인증서·키 자료를 Galaxy Knox와 연계(백업·증명) → **SoC 키스토어 + Knox 이중 보호**, 엔터프라이즈/기업 고객 대응.

---

### 지연 예산 — 종단간 ~3초 `firmware.html §12 / server-api §10`

| 구간 | 예산 |
|------|------|
| 버즈 Opus 인코딩 + 패킷 빌드 | <300 ms |
| 버즈 → 호스트 (BT, **~200KB** = A-int+A-ext+B) | **~1700 ms** |
| 호스트 → 서버 (인터넷) | <500 ms |
| 서버 AI 처리 (STT~핸들러~LLM첫토큰~TTS첫청크) | <1000 ms (p50) / <2000 ms (p95) |
| 응답 → 호스트 → 버즈 재생 시작 | ~500 ms |
| **합계** | **~3.3초** (외부 무관 시 생략하면 ~3초) |

---

### Galaxy 생태계 — 옵션 레이어 `proposal / host-bridge / scenarios`

- **컴패니언 앱**(선택): JWT로 read-only API, 본인 히스토리/통계/위젯 — **버즈 동작에 불필요**
- **자동 라우팅**: 응답을 Spotify·Samsung Notes·Calendar·Internet 등 적절한 앱으로 디스패치 (`x-galaxy-intent` 메타)
- **Bixby Routines**: 위치·시간 기반 BudsAI 모드 자동 트리거
- **SmartThings / One UI Auto / DeX / Watch / Tab** 다중 디바이스 동기화
- **비-Galaxy 사용자도 모든 핵심 Q&A 풀기능** 유지 — 락인은 메커니즘이 아니라 UX 차원

---

## 3. 같이 확정할 것 (열린 질문)

1. ✅ **코덱 = LC3로 결정** (Opus→LC3). 비교 범위 = **버즈4 프로 지원 4종(SBC/AAC/SSC UHQ/LC3)** → 이 안에서 "압축률 1등 = LC3" 정확. 남은 확인: 비트레이트 값(Track A 32 / Track B 16 kbps) 확정.
2. ⚠️ **불일치 2** — 의도 개수: "8개" vs "8+3" → 슬라이드 카피 통일
2.5 ⚠️ **불일치 3** — Galaxy 경로 TTS 생성 위치: 이전 다이어그램은 **폰(VITS)**, server-api 원문은 **서버**. → 어느 게 맞는 설계인지 확정 (§0.5 참고)
3. 슬라이드 **분량**: 10장 그대로 vs 7장 압축?
4. 청중: **내부 기술리뷰**인지 / **경영·투자 발표**인지 → 깊이(패킷 바이트 레벨까지 vs 흐름 위주) 결정
5. 비주얼 톤: 기존 compare-slides의 화이트/티얼 톤 그대로 재사용? (style.css + is-deck-light 패턴 이미 있음)
6. 결과물 포맷: HTML 슬라이드(?present 모드) + SVG 다이어그램 — 기존 `_gen_*_svg.py` 패턴 재사용?
