# BudsAI 장표(`slides-content.md`) 비판 — 레드팀 / 블루팀

> 대상: `slides-content.md` (S01~S07)
> 대조 근거: `architecture.md` (특히 §0.5 "두 갈래 경로")
> 작성일: 2026-05-30
> 형식: **① 사실·정합성 오류 → ② 공격(레드팀) → ③ 방어(블루팀, 자가 반박 포함) → ④ 우선 수정 권고**

---

## 0. 총평

기술 빌드업과 비유는 훌륭하다(특히 LC3 원리, mTLS 자판기 비유, BNEP 택배 비유). 다만 **장표가 근거 문서보다 한 톤씩 더 강하게 단정**하는 곳이 많고, 그 결과 *근거 문서 자신과 모순*되거나 *법적·물리적으로 위험한 주장*이 섞여 있다. 핵심 결함은 세 가지다:

1. **S01이 "두 갈래 경로"를 단일 경로로 뭉갰다** → 프라이버시 카피가 근거 문서와 충돌.
2. **상시 양채널 녹음의 법적·DRM·전력 리스크를 장표가 전혀 다루지 않는다.**
3. **여러 "정확히 / 못 봄 / 못 베낌 / 0" 같은 절대 단정**이 과장이다.

---

## 1. 사실·정합성 오류 (그냥 틀렸거나 문서끼리 안 맞는 것)

### 1-1. ⛔ **S01 프라이버시 카피가 `architecture.md` §0.5와 정면 모순** (최우선)
- S01 line 59: *"거쳐 가는 기기는 **내용을 못 봐요.**"* / line 37: 흐름을 `mTLS → BNEP`로 단정.
- 그러나 `architecture.md` §0.5 표는 **🔵 갤럭시 폰 경로 = 폰이 페이로드를 "본다"**(자사 기기라 신뢰), **로컬 SQLite에 저장**, **폰에서 TTS 생성**이라고 명시.
- 즉 S01은 **비-갤럭시(dumb pipe) 경로 하나만** 그려 놓고 그걸 전체인 양 말한다. 삼성 입장에서 *주 사용자(갤럭시 유저)*는 폰이 내용을 보는 경로인데, 장표는 "아무도 못 본다"고 단언 → **거짓**.
- S01은 또 `mTLS → BNEP`를 기본 흐름으로 박았지만, 갤럭시 경로는 **BNEP IP 라우팅이 아니라 BT 오디오 + HTTPS/JWT**다. 전송 메커니즘 자체가 다른데 하나로 합쳐 그렸다.

> **결론**: S01은 "두 갈래"가 드러나게 다시 그리거나, 최소한 "(비-갤럭시 경로 기준)"이라고 못박아야 한다. 현 상태로 발표하면 §0.5와 모순되는 두 장표를 동시에 내놓는 셈.

### 1-2. CRC를 보안(변조 방지)인 것처럼 제시
- S05 line 199/216: CRC를 "오류검출 / 도중에 안 깨졌는지 확인"으로 SBAP 푸터에 박고, AEAD·서명은 "옵션"(architecture line 466).
- **CRC는 우연한 손상 검출용이지 변조(tamper) 방지가 아니다** — 공격자가 내용을 바꾸고 CRC를 다시 계산하면 끝. 무결성은 TLS의 AEAD가 보장한다.
- 장표가 보안 맥락에서 CRC를 무결성 장치처럼 보이게 두면 오해를 부른다. "무결성은 mTLS/AEAD, CRC는 전송 잡음 검출용"으로 역할을 분리 표기해야 함.

### 1-3. "주파수 호핑 → 도청 회피"는 통념상 과장
- S05 line 192: *"초당 ~1600번 채널을 바꿔 뜀(주파수 호핑) → 간섭·도청 회피."*
- AFH의 본래 목적은 **간섭 회피**다. 광대역 SDR을 든 공격자에겐 호핑이 실효 도청 방어가 못 된다. 실제 도청 방어는 **링크레벨 AES + mTLS**다(이건 문서에 제대로 있음).
- "도청 회피"는 빼고 "간섭 회피"로 정정하는 게 정직.

### 1-4. "재압축 0 / 트랜스코딩 0"이 대표 사례가 아닌데 헤드라인
- S01 line 16, S02 line 93, S05 장점 ④: "내부음이 이미 LC3(LE Audio)면 그대로 저장 → 재압축 0"을 효율의 간판으로 사용.
- 그러나 (a) 현 갤럭시 다운링크는 상당수가 **A2DP(SBC/AAC/SSC)** — LE Audio LC3로 들어오는 건 아직 *예외*에 가깝다. (b) 들어오는 네이티브 LC3는 폰이 고른 **임의 비트레이트/샘플레이트/스테레오**(예: 48kHz·80~160kbps stereo)일 수 있어, "그대로 저장하면 30초 90KB"라는 24kbps mono 예산과 **안 맞는다**. 그대로 저장하면 용량이 튀고, 예산 맞추려면 결국 디코드→재인코딩이 필요.
- 즉 "트랜스코딩 0"은 **소수 케이스에만 성립**하는데 장표는 일반 원칙처럼 말한다. → "LE Audio LC3로 들어올 때에 한해"라는 단서가 필요.

### 1-5. 샘플레이트 혼재가 설명 없이 섞임
- S02 ②표는 모든 채널을 48kHz·16bit로 PCM 환산(96KB/s)하는데, Track B 발화 5초는 0.16MB(=16kHz 기준)로 계산됨(architecture line 91 "16kHz wideband").
- 컨텍스트(A)는 48kHz, 발화(B)는 16kHz라는 전제가 장표엔 안 보인다. 틀린 건 아니지만 표만 보면 앞뒤가 안 맞아 보인다 → 각 행에 SR 주석 필요.

### 1-6. 압축비 수치 불일치 (사소)
- S02 line 101: "데이터는 1/20". 그러나 표상 48kHz 컨텍스트는 2.88MB→90KB = **약 1/32**. architecture는 "1/16~1/24"라 적음. 셋이 제각각. 큰 문제는 아니나 한 값으로 통일.

### 1-7. DMA "같은 주소를 읽기만"의 단순화
- S03: ANC 엔진과 LC3 인코더가 "같은 주소"를 읽는다고 강조.
- 현실에선 **ANC는 수십 µs 지연의 라이브 ADC 스트림**을 보고, 30초 링버퍼는 그 하류다. 둘이 *물리적으로 동일 주소*를 공유한다기보다 "한 번 캡처한 데이터를 두 소비자가 본다"가 정확. 비유로는 OK지만 "같은 주소"는 기술 청중에게 트집 잡힌다.

---

## 2. 공격 (레드팀) — 발표장에서 들어올 칼

### 2-A. 🔴 상시 양채널 녹음 = 법적·프라이버시 지뢰 (가장 강한 공격)
- S01 "늘 기억하기": **외부 마이크 주변음을 상시 30초 버퍼링** = 곁에 있는 *타인의 대화를 동의 없이 상시 녹음*하는 것.
- 미국 양자 동의(two-party consent) 주, EU GDPR, 한국 통신비밀보호법 관점에서 **방관자(bystander) 동의 부재**가 정면 문제. 30초 링버퍼라도 "트리거 시 서버로 전송"되는 순간 *처리·전송*이 발생한다.
- 장표는 이걸 "늘 귀를 열어둡니다"로 **밝고 무비판적으로** 서술. 발표장에서 법무/규제 담당이 첫 질문으로 던질 지점인데 방어 슬라이드가 없다.
- (참고: repo 최근 커밋에 "consent toast / legal notice"가 있는 걸 보면 팀도 인지 중. 그런데 **장표 본문엔 한 줄도 없음** → 정합성 구멍.)

### 2-B. 🔴 내부음(이어폰 출력) 캡처 = DRM/저작권 노출
- A-int = Spotify/Netflix 등 **저작권 보호 콘텐츠의 복호된 오디오**를 떠서 서버로 전송("무슨 곡/대사인지 식별·번역").
- 이는 *콘텐츠 보호 우회 + 저작물 복제·전송* 소지. 24kbps라도 "원본을 떠서 외부 서버로 보낸다"는 행위 자체가 문제.
- 장표는 이걸 "헛일 안 하기(그대로 담아둠)"라는 **효율 자랑으로만** 다룬다 → 법적 리스크 무방비.

### 2-C. 🔴 "아무 기기나 통로로" 과장 — 차량·TV·iPhone은 대부분 불가
- S06 line 242/272: *"노트북·차량·TV 등 아무 기기나 통로로 / 호스트 무관."*
- 현실: **BT PAN의 NAP(인터넷 공유) 역할**을 실제로 해주는 건 데스크톱 OS·안드로이드 정도다. **자동차 인포테인먼트·스마트TV는 BNEP NAP로 인터넷을 공유하지 않는다.** iPhone도 임의 기기에 BT PAN 테더링을 잘 안 열어준다(architecture는 이걸 Tier 2/3 fallback으로 인정).
- 즉 근거 문서는 "90%+ desktop/Android"로 신중한데, **S06이 "차량·TV·아무거나"로 부풀렸다.** 데모에서 렌터카에 꽂아보라고 하면 깨질 주장.

### 2-D. 🟠 전력·발열 — 상시 듀얼채널 캡처+LC3 인코딩의 비용
- architecture는 BUFFERING ~15mA "상시"라 적었지만, **두 채널 PCM 캡처 + LC3 인코딩을 24시간 돌리면** 콩알만 한 버즈 배터리(수십 mAh급)에선 재생시간이 눈에 띄게 깎인다.
- 장표는 "겉으론 쉬는 것 같지만 계속 돌고 있다"고 *자랑*만 하고 **배터리 대가를 언급 안 함**. "음악도 안 트는데 왜 배터리가 빨리 닳지?"가 사용자 체감 공격 포인트.

### 2-E. 🟠 가속도계 VAD "정확히"는 과장 — 동작 아티팩트
- S01 line 57 / S04 line 167: *"음악이 깔려 있어도 내 말이 끝난 걸 **정확히** 안다."*
- 가속도계/골전도는 **발화 존재 감지엔 강하지만 정밀 엔드포인팅엔 약점**: (a) 무성음·속삭임·문장 끝 흐려지는 구간은 진동이 약해 일찍 끊김, (b) **씹기·걷기·고개 끄덕임 같은 동작 진동**이 "아직 말하는 중"으로 오검출되거나 반대로 잡음, (c) 대역이 좁아 시점 해상도가 마이크보다 거칠다.
- "정확히"는 "재생음·주변소음에 **강인하게**"로 약화해야 방어 가능. (강인성은 진짜 강점, *정밀도*는 별개.)

### 2-F. 🟠 "사설 프로토콜 = 해자/IP, 경쟁사가 못 베낌" — 가짜 해자
- S05 line 230 / architecture 장점 7: *"복제 난이도 = 해자/IP → 경쟁사가 못 베낌."*
- 사설 페이로드 포맷은 **기기에서 트래픽/펌웨어 리버싱으로 쉽게 추출**된다. 포맷의 비밀성은 보안도 해자도 아니다(진짜 방어는 mTLS). "프로토콜이 비밀이라 못 베낀다"는 *security/moat through obscurity* — 발표장에서 바로 반박당함.
- 진짜 해자는 칩-앱-서버 *수직통합 + 인증서 발급 인프라*지, 포맷 자체가 아니다. 카피를 그쪽으로 옮겨야 함.

### 2-G. 🟡 매 질의마다 30초 환경음 서버 전송 = 데이터 노출량
- "의도 분류 100% 서버"이므로 **질문 1건마다 내부+외부 30초(~180KB)가 통째로 서버로** 나간다. 단순 "지금 몇 시야?"에도 주변 30초가 업로드됨.
- 프라이버시 관점 공격: 필요 없는 컨텍스트까지 상시 유출. → "온디바이스 1차 의도 게이팅으로 불필요한 컨텍스트 전송 억제" 같은 방어 설계가 장표에 없다.

### 2-H. 🟡 S07 "칩을 뜯어도 못 빼감" — 근거 문서보다 과장
- S07 line 311: *"키가 펌웨어에 없으니 **칩을 뜯어도 못 빼감.**"*
- architecture(line 542, 567)는 정직하게 *"랩 수준 물리 공격은 가능, 단 1대뿐이라 경제적으로 무의미"*, *"디캡·부채널 저항은 EAL6 SE보다 약함"*이라 인정.
- 장표가 "못 빼감"으로 단정하면 보안 청중에게 신뢰를 잃는다. 근거 문서의 정직한 톤("뚫어도 그 기기 1대 + 즉시 폐기")으로 맞춰야 한다.

---

## 3. 방어 (블루팀) — 살아남는 주장 + 위 공격에 대한 반박

### 3-1. ✅ LC3 코덱 선택 — 견고함 (공격 2-? 없음)
- "**버즈4 프로 지원 4종(SBC/AAC/SSC UHQ/LC3) 중 저비트레이트 압축효율 1등 = LC3**"는 범위를 명시한 **정확한 카피**다(architecture가 비교 범위를 정직하게 한정). Opus·LDAC·aptX는 미지원이라 논외라고 선긋는 것도 옳다.
- LE Audio 필수 코덱이라 **칩 내장·추가 라이선스 0**, UL/DL 양방향이라 컨텍스트(DL)+발화(UL) 한 코덱 통일 — 사실 기반. **방어 성공.**

### 3-2. ✅ mTLS + SoC 키스토어 — 산업 표준에 부합
- 쓰기 전용 키 금고(KMU), 시큐어부트, 기기별 인증서·개별 폐기는 **표준 IoT 보안 모범사례**. 실제 부품 근거(Nordic nRF5340, Qualcomm QCC)도 실재.
- 2-H 공격에 대한 반박: 위협 모델이 "음성+API 접근"이라 EAL6 독립 SE는 과잉이라는 §2.5 논리는 타당. **단, S07 카피를 "뜯어도 못 빼감"→"뜯어도 그 1대뿐 + 즉시 폐기"로 정직화하면** 방어 완성.

### 3-3. ✅ 가속도계 VAD의 *강인성* — 진짜 강점
- 2-E 공격(정밀도)과 별개로, **재생음·주변소음 면역**은 실재하며 양산 사례(AirPods Conversation Awareness, 갤럭시 Voice Detect)가 뒷받침. "마이크 에너지 VAD가 음악/소음을 '말하는 중'으로 오인"하는 문제를 센서로 우회한다는 핵심 논리는 옳다.
- 방어 멘트: *"끝점 시각의 µs 정밀도가 아니라, 시끄러운 환경에서 '내 발화'만 분리하는 강인성이 목적"* — 이렇게 프레이밍하면 2-E를 무력화.

### 3-4. ✅ BNEP/PAN dumb-pipe + E2E — 아키텍처적으로 깔끔
- "전송은 표준, 의미는 사설"과 OS 빌트인 테더링 활용은 앱설치 0의 정당한 근거. 데스크톱/안드로이드에선 실제 동작.
- 2-C 공격에 대한 반박: **"차량·TV·아무거나"를 "데스크톱·안드로이드·인터넷 공유 가능 호스트"로 좁히고, 나머지는 Tier 2/3 fallback으로 정직 표기**하면 방어 가능. 즉 주장을 *축소*하면 살아남는다.

### 3-5. ✅ 서버측 의도 분류 — 펌웨어 단순·OTA 불요
- 버즈에 의도 로직 0 → 의도 추가 시 서버만 갱신, 전 기기 OTA 불필요. 합리적 분업. (2-G의 "불필요 컨텍스트 전송" 문제는 *온디바이스 게이팅* 한 줄 추가로 보완하면 됨 — 설계 자체는 유효.)

### 3-6. ✅ DMA "한 번 쓰고 둘이 읽는다" — 임베디드 정석
- 1-7에서 "같은 주소"는 단순화지만, **복사 없이 한 캡처를 ANC와 인코더가 공유**한다는 큰 그림은 맞다. "동일 주소" 문구만 "한 번 캡처→두 소비자"로 다듬으면 방어 완성.

### 3-7. ✅ 운영자 이득 분리(보안 §4)는 오히려 모범
- architecture가 "보안의 진짜 수혜자는 운영자, 사용자 체감은 3개뿐"이라 **정직하게 분리**한 건 발표 신뢰도를 높이는 강점. 이 톤을 S07 본문에도 유지하면 좋다.

---

## 4. 우선 수정 권고 (영향도 순)

| 순위 | 항목 | 조치 |
|---|---|---|
| 1 | **S01 단일경로 모순 (1-1)** | "두 갈래" 드러내거나 "(비-갤럭시 경로)" 명시. 폰 경로는 폰이 페이로드를 본다는 사실을 §0.5와 일치시킬 것 |
| 2 | **상시 녹음 법적 리스크 (2-A/2-B)** | 동의·DRM·데이터 최소화 방어 슬라이드 1장 추가(이미 consent toast 작업 중이니 본문에 반영) |
| 3 | **"아무 기기나" 과장 (2-C)** | "데스크톱·안드로이드 등 인터넷 공유 가능 호스트"로 축소 + fallback 표기 |
| 4 | **CRC=보안 오해 (1-2)** | 무결성=mTLS/AEAD, CRC=전송잡음 검출로 역할 분리 |
| 5 | **과장 단정 정직화 (2-E/2-F/2-H/1-3)** | "정확히"→"강인하게", "못 베낌"→"수직통합 우위", "뜯어도 못 빼감"→"1대 한정+폐기", "도청회피"→"간섭회피" |
| 6 | **트랜스코딩 0 단서 (1-4)** | "LE Audio LC3로 들어올 때에 한해" 조건 명시 + 비트레이트/SR 정합 |
| 7 | **수치 정합 (1-5/1-6)** | 채널별 SR 주석, 압축비 한 값으로 통일 |
| 8 | **전력 대가 (2-D)** | 상시 버퍼링의 배터리 영향 한 줄 정직 표기 |

---

### 한 줄 요약
기술 코어(LC3·mTLS·가속도계 VAD·BNEP)는 **범위를 정직하게 좁히면 거의 다 방어된다.** 진짜 위험은 *기술*이 아니라 **① S01이 두 경로를 뭉개 만든 프라이버시 거짓말**과 **② 상시 녹음의 법적·DRM 무방비**다 — 이 둘은 축소가 아니라 *별도 방어 설계*가 필요하다.

---

## 5. `architecture.md` 자체 비판 — "근거 문서"도 믿지 마라

위 1~4장은 `architecture.md`를 사실 기준으로 깔고 썼다. 그런데 **그 문서 자체가 검증된 근거가 아니다.** 따라서 1~4장의 "근거 문서와 일치하니 OK"라는 판정도 그만큼만 신뢰해야 한다.

### 5-0. 🔴 메타 결함 — 자기참조 + 발명된 스펙
- 문서 머리말(line 5): 출처 = *"proposal / packet / firmware / host-bridge / server-api / … (repo 내 기존 슬라이드)."* → **외부 권위 출처가 아니라 자기 프로젝트의 이전 장표를 인용**한다. 즉 "근거"가 **순환 참조**다. 슬라이드 A가 슬라이드 B를 인용하고 B는 A 계열에서 나왔다.
- 웹으로 실제 검증된 건 일부(LC3 개요, VAD 부품 사례)뿐. 나머지 **수치 대부분은 측정값이 아니라 피치용으로 만든 추정**이다:
  - 상태별 전류 `IDLE 2mA / BUFFERING 15mA / RECORDING 25mA …`(line 107) — 출처 없는 발명값.
  - 패킷 바이트 레이아웃(헤더 16B, TLV 필드들, line 437~444) — 측정 아닌 설계 가안.
  - 지연 예산 ms 분해(line 661~668) — 근거 없는 낙관 추정.
- **결론: architecture.md는 "검증된 사양서"가 아니라 "정합성 검토 중인 설계 초안"이다.** 머리말도 "초안"이라 자인. 이걸 사실로 인용하면 1~4장 방어 판정도 모래 위 집.

### 5-1. 🔴 문서가 *스스로 인정한* 미해결 모순이 3건
- `⚠️ 불일치 2`(line 499/685): 의도 "8개" vs "8+3" — 미정.
- `⚠️ 불일치 3`(line 56/686): 갤럭시 경로 TTS를 **폰(VITS)**이 만드나 **서버(Opus)**가 만드나 — 미정. 그런데 §0.5 표와 다이어그램은 이미 "폰 VITS"로 그려 놓음 → **확정 안 된 걸 확정인 양 도식화.**
- `⚠️ 불일치 3`이 "이전 불일치들"을 언급(→ **불일치 1**도 어딘가 존재) → 내가 읽은 범위 밖에 모순이 더 있다는 자백.
- 즉 **이 문서를 근거로 쓴 모든 장표는 미해결 분기 위에 서 있다.** S01이 "서버가 보낸 답"으로 그린 것도 불일치 3을 임의로 한쪽으로 정한 것.

### 5-2. 🔴 검증 불가/날조 의심 하드웨어 주장
- **"버즈4 프로 코덱 지원 = SBC·AAC·SSC UHQ·LC3 (Bluetooth 6.1)"**(line 164): "Buds4 Pro"의 실재·스펙·"블루투스 6.1 탑재"는 **확인 불가**. 이어버드에 "BT 6.1"이라 박는 것 자체가 의심스럽다(코어 스펙 버전과 제품 마케팅 혼동). 전 코덱 비교론(2-?·3-1)이 이 미확인 전제 위에 있다.
- **"현 갤럭시 버즈(Pro) = Broadcom BCM43015 + 외부 NOR 플래시"**(line 537): 특정 칩 지목인데 문서 스스로 **"Buds4 Pro 분해도는 비공개"**라 인정 → 곧 *출처 없는 단정*. 이 "현 상태=보안 약함" 주장이 §2.5 "그러니 보안 SoC로 세대교체" 논리의 발판인데, 발판이 미검증.
- **부품-제품 짝짓기**(line 564): "nRF5340 → Ultrahuman Ring Air / NNOXX One 양산 탑재" 같은 구체 매칭은 **사실 확인 필요**(칩-제품 대응이 틀리면 근거 전체가 흔들림). 인용처럼 보이지만 링크가 아니라 단정.

### 5-3. 🟠 내부 정합성 자기모순 (문서가 못 잡은 것)
- **지연 예산표(line 663)**: *"버즈 **Opus** 인코딩 + 패킷 빌드 <300ms"* — 문서 결론은 "전 계층 LC3로 교체"인데 예산표는 **여전히 Opus**. 코덱 교체 후 카피 일괄 수정이 안 끝난 stale 값. 즉 표의 숫자들도 옛 가정 기반일 수 있음.
- **BT 전송 ~200KB = ~1700ms**(line 664): 실효 ≈0.94Mbps 가정. BNEP/L2CAP 오버헤드·재전송·ANC와의 SoC 자원 경합을 빼면 **낙관치**. 슬라이드는 "~2초"로 반올림하지만 실제론 더 길 수 있다.
- **전체 ~3.3초**: 음성 어시스턴트로는 느린 편인데, STT "Whisper급 150~250ms"는 **서버 큐·콜드스타트·네트워크 변동 무시한 베스트케이스.** p95 2초는 더 커질 여지.

### 5-4. 🟠 펌웨어 자원 예산 낙관
- §Layer 2b(line 341): BNEP+lwIP+mbedTLS+DNS/DHCP+HTTP = **"~150~250KB 플래시"**로 수용 가능하다 함.
- 그러나 (a) 이건 **플래시(코드)만** — TLS 1.3 핸드셰이크·인증서·세션버퍼의 **RAM** 예산은 빠짐. 작은 Cortex-M에선 RAM이 더 빡빡하다. (b) 그 위에 **ANC 실시간 필터 + 듀얼 LC3 인코딩 + 30초×2 링버퍼 + DMA**를 동시에 돌린다. 단일 버즈 SoC에서 이 전부의 **동시 연산·메모리 예산**이 맞는지는 문서가 증명 안 함. "수용 가능"은 희망.

### 5-5. 🟠 feasibility 과욕 — eSIM Tier 4
- §Layer 2 Fallback Tier 4(line 322): *"버즈 내장 eSIM(NB-IoT/LTE-M), 호스트 불요, 프리미엄."*
- 이어버드에 셀룰러 모뎀+eSIM+안테나를 넣는 건 **전력·발열·크기·인증(통신사) 측면에서 비현실적**. 피치에 "옵션"으로 끼워 넣었지만 데모/양산 근거 0. 들어오면 바로 깨질 항목.

### 5-6. 🟡 "마케팅 수치"를 사실처럼
- **"LC3 160kbps > SBC 345kbps"**(line 170): Bluetooth SIG의 청취 테스트 **마케팅 주장**을 하드 팩트처럼 인용. 조건부(특정 테스트셋)임을 안 밝힘.
- **"Spotify 데이터 절약 ≈ 24kbps"·"HD 통화 13~24kbps"**(line 143~145): 대략 맞지만 출처 없는 어림. "24kbps = sweet spot" 결론을 떠받치는데 근거는 비유 수준.
- **"Shazam도 16kHz 저대역만 본다"**(line 150): 핑거프린팅은 보통 ~5kHz대 스펙트럼 피크를 쓴다. "16kHz 저대역"은 부정확. 게다가 **lossy 24kbps가 마스킹된 성분을 버리면** 핑거프린트 강인성이 오히려 떨어질 수 있는데, 문서는 "차고 넘침"이라 단정.

### 5-7. 🟡 운영 인프라 비용 누락
- 기기별 X.509 발급은 **공장 라인에서 버즈마다 온칩 키 생성 + CA 서명 + 프로비저닝**, 그리고 **OCSP/CRL 폐기 인프라**를 수백만 대 규모로 운영해야 한다. §키 수명주기는 "폐기 목록에 한 줄"이라 *간단해 보이게* 썼지만, **PKI 운영 비용·실패 모드(CA 키 유출 시 전수 재발급)**는 빠졌다. 보안 §4는 "운영자 이득"은 길게 쓰면서 **운영자 비용**은 안 쓴다 → 한쪽만 본 정직.

### 5-8. 정리 — architecture.md를 어떻게 취급할까
| 신뢰 등급 | 항목 |
|---|---|
| **🟢 외부 검증됨(상대적 신뢰)** | LC3가 LE Audio 필수 코덱·칩 내장 / 가속도계 발화감지 양산 사례(AirPods·Voice Detect) / TLS·mTLS 일반 원리 |
| **🟡 그럴듯하나 미검증(인용 금지)** | 코덱 비교 수치, 24kbps 일상 비유, 펌웨어 자원 예산, 지연 ms 분해 |
| **🔴 발명/미확인(피치 전 검증 필수)** | Buds4 Pro 코덱·BT6.1, 현행 칩(BCM43015), 부품-제품 매칭, 전류 mA, 패킷 바이트, eSIM Tier4 |
| **⛔ 문서가 자인한 미해결** | 불일치 1·2·3(특히 TTS 생성 위치) |

> **메타 결론**: `architecture.md`는 *근거*가 아니라 *주장*이다. 자기 repo를 순환 인용하고, 발명한 수치를 사양처럼 적고, 미해결 모순 3건을 안은 초안이다. 따라서 1~4장의 "근거 문서와 일치 → 방어 성공" 판정 중 상당수는 **"두 초안이 서로 일치"**에 불과하다. 발표 전에 🔴/🟡 항목은 **외부 1차 출처(SIG 스펙, 실측 분해, 데이터시트)로 재검증**하거나, 못 하면 장표에서 단정을 빼고 "설계 가정"이라 표기해야 한다.
